{
  "name": "@erebuz/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Erebuz smart contracts (Hardhat) — tests and tooling share @erebuz/sdk.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "build": "hardhat compile",
    "typecheck": "tsc --noEmit",
    "deploy:local": "tsx scripts/deploy.ts"
  },
  "dependencies": {
    "@erebuz/sdk": "workspace:*"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20",
    "chai": "^4.5.0",
    "hardhat": "^2.22.17",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
